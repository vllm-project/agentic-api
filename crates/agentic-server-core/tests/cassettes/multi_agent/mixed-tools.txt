Complete three independent jobs, assigning a different subagent to each so they can work in parallel: (1) use web search to find Python's official documentation for the csv module and summarize how it reads CSV rows, citing the source; (2) use the gitmcp_tiktoken MCP server's search_tiktoken_documentation tool to find how tiktoken encodes text into tokens and decodes tokens back into text, and summarize the returned documentation; (3) use the local shell tool with this exact command: python3 -c 'print(sum(range(1, 11)))'. The shell environment is Linux bash with Python 3. Wait for the client to return shell_call_output before reporting the calculation's result. Collect the three agents' findings and provide one combined answer with a section for each job. Use the requested tools rather than answering from memory or inventing tool outputs. If a tool fails, report the observed failure.
