# docker compose -f deploy/otel/docker-compose.yaml --env-file deploy/otel/jaeger.env up -d
# Starts Jaeger (UI on http://127.0.0.1:16686) and sends traces to it as well.
COMPOSE_PROFILES=jaeger
AGENTIC_OTEL_TRACE_EXPORTERS=[debug, otlp/jaeger]
